name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        # The oldest supported Go and the current one. The broker embeds a NATS
        # server in its tests, so the matrix also catches nats-server breaking
        # on a newer toolchain.
        go: ['1.25', 'stable']
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}
          check-latest: true

      - name: Verify go.mod and go.sum are tidy
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        run: go test -race -count=1 ./...

  fuzz:
    # The decoder parses bytes from unauthenticated peers, so every push gets a
    # short fuzzing run on top of the seed corpus.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: 'stable'
          check-latest: true

      - name: Fuzz the packet decoder
        run: go test ./packet/ -run FuzzRead -fuzz FuzzRead -fuzztime 60s

      - name: Fuzz the topic mapping
        run: go test ./topic/ -run FuzzSubjectRoundTrip -fuzz FuzzSubjectRoundTrip -fuzztime 60s

  docker:
    # Builds the image and runs the quickstart from the README against it, so
    # a broken Dockerfile or compose file fails the pull request rather than
    # the release.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - name: Build the image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: natsmqtt5:ci
          build-args: VERSION=ci-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: The entrypoint runs
        run: docker run --rm natsmqtt5:ci -version

      - uses: actions/setup-go@v6
        with:
          go-version: 'stable'
          check-latest: true

      - name: Start NATS and the broker
        run: docker compose -f compose.yaml -f compose.ci.yaml up -d

      - name: Wait for the broker
        run: .github/scripts/wait-for-tcp.sh 127.0.0.1 1883 60

      - name: Smoke test the running image
        env:
          NATSMQTT5_SMOKE_ADDR: 127.0.0.1:1883
        run: go test -run TestSmoke -count=1 -v .

      - name: Logs
        if: always()
        run: docker compose -f compose.yaml -f compose.ci.yaml logs
